---
layout: base.njk
---

<section class="hero">
  <div class="container">
    <h1>نور الإيمان</h1>
    <p class="subtitle">رحلة روحانية في عالم متغير</p>
    <a href="/articles/" class="cta-button">تصفح المقالات</a>
  </div>
</section>

<section class="featured-posts">
  <div class="container">
    <h2 class="section-title">أحدث المقالات</h2>
    
    <div class="posts-grid">
      {% if collections.posts.length > 0 %}
        {% for post in collections.posts | reverse | limit(3) %}
          <div class="post-card reveal">
            {% if post.data.thumbnail %}
              <div class="thumbnail">
                <img src="{{ post.data.thumbnail }}" alt="{{ post.data.title }}">
              </div>
            {% endif %}
            
            <div class="content">
              {% if post.data.category %}
                <div class="category">{{ post.data.category }}</div>
              {% endif %}
              
              <h3 class="title">
                <a href="{{ post.url }}">{{ post.data.title }}</a>
              </h3>
              
              {% if post.data.excerpt %}
                <p class="excerpt">{{ post.data.excerpt }}</p>
              {% endif %}
              
              <div class="meta">
                <span class="date">{{ post.date | formatDate }}</span>
                {% if post.data.author %}
                  <span class="author">{{ post.data.author }}</span>
                {% endif %}
              </div>
              
              <a href="{{ post.url }}" class="read-more">اقرأ المزيد →</a>
            </div>
          </div>
        {% endfor %}
      {% else %}
        <div class="no-posts">
          <p>لا توجد مقالات بعد. ترقبوا المزيد قريبًا!</p>
        </div>
      {% endif %}
    </div>
    
    {% if collections.posts.length > 3 %}
      <div class="view-all">
        <a href="/articles/" class="cta-button">عرض جميع المقالات</a>
      </div>
    {% endif %}
  </div>
</section>

<section class="hadith-section">
  <div class="container">
    <h2 class="section-title">حديث اليوم</h2>
    
    <div class="hadith-card reveal">
      <blockquote>
        <p>{{ metadata.dailyHadith.text }}</p>
        <cite>{{ metadata.dailyHadith.source }}</cite>
      </blockquote>
    </div>
  </div>
</section>

{% if collections.categories.length > 0 %}
<section class="categories-section">
  <div class="container">
    <h2 class="section-title">تصفح حسب التصنيف</h2>
    
    <div class="categories-grid">
      {% for category in collections.categories %}
        <div class="category-card reveal">
          <div class="icon">
            <i class="fas fa-book-open"></i>
          </div>
          <h3>{{ category }}</h3>
        </div>
      {% endfor %}
    </div>
  </div>
</section>
{% endif %}
